{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "regenerate proto",
            "type": "shell",
            "command": "protoc -Iproto --python_out=./proto --pyi_out=./proto ./proto/request.proto",
            "problemMatcher": []
        },
        {
            "label": "reformat",
            "type": "shell",
            "command": "poetry run ruff format src tests scripts",
            "problemMatcher": []
        }
    ]
}